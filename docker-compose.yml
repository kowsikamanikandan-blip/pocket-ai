services:
  pocketsmart:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - pocketsmart_data:/app/data
    environment:
      DATABASE_URL: sqlite:////app/data/pocketsmart.db
volumes:
  pocketsmart_data:
